{
  "name": "ondevaipassar",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "backend",
    "frontend",
    "packages/*"
  ],
  "scripts": {
    "postinstall": "npm run build --workspace=packages/shared"
  }
}
